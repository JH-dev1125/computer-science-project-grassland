# =============================================================================
# main.py — 프로그램 최상위 진입점 (가장 먼저 실행되는 파일)
# =============================================================================
#
#  ┌───────────────────────────────────────────────────────────────────────┐
#  │  이 프로젝트의 주석 표기 규칙 (모든 파일 공통)                          │
#  │    # [흐름 N] : 실행 순서 단계 번호                                     │
#  │    # [호출→] : 이 코드가 '다음에' 부르는 함수/메서드                    │
#  │    # [←호출] : 누가(어디서) 이 코드를 부르는가                          │
#  │    # [변수]   : 변수 이름의 의미                                        │
#  │    # [문법]   : 생소한 파이썬 문법의 명칭과 그것을 쓰는 이유            │
#  └───────────────────────────────────────────────────────────────────────┘
#
#  [전체 실행 흐름 한눈에 보기]  python main.py 를 치면 →
#    1) main.py            : run() 호출만 한다 (이 파일)
#    2) app.py    run()    : 명령줄 인자 파싱 → World 생성 → GUI 실행
#    3) world.py  World.seed_default() : 맵에 동·식물·지형을 배치
#    4) gui.py    GrasslandApp(world).run() : 창을 열고 '매 프레임 루프'를 돈다
#         루프 안에서 매 프레임 →  world.update(dt)  (시뮬레이션 한 칸 진행)
#                              →  gui.draw()         (화면에 그림)
#    5) world.update(dt)  : 환경→식물→동물→물리→자원→번식/사망 순서로 진행
#  이 파일부터 위 순서대로 주석을 따라 읽으면 프로그램 흐름을 그대로 따라갈 수 있다.
# =============================================================================

# [문법] from A import B : A 모듈(파일/패키지) 안의 이름 B 만 골라 가져온다.
#        여기서는 grassland 패키지의 app.py 안에 정의된 run 함수를 가져온다.
#        이 import 문이 실행되는 순간 app.py 의 맨 위(import argparse 등)도 함께 실행된다.
# [흐름 0] 파이썬이 main.py 를 읽기 시작하면 가장 먼저 이 import 가 수행된다.
from grassland.app import run

# [문법] if __name__ == "__main__":
#        파이썬은 파일을 '직접 실행'하면 그 파일의 __name__ 변수에 문자열 "__main__" 을 넣고,
#        다른 파일이 'import' 만 하면 모듈 이름("main")을 넣는다.
#        → 즉 "이 파일을 python main.py 로 직접 실행했을 때만 아래를 수행하라"는 관용구.
#        (import 당할 때는 run() 이 저절로 호출되지 않게 막아 주는 안전장치)
if __name__ == "__main__":
    # [흐름 1] 프로그램의 실질적 시작점. 모든 동작은 app.py 의 run() 안으로 들어간다.
    # [호출→] grassland/app.py 의 run()
    run()
